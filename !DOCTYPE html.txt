<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Carta secreta 💌</title>
  <style>
    body {
      font-family: 'Arial', sans-serif;
      background: #ffeaf0;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
      overflow: hidden;
    }

    h1 {
      margin-bottom: 20px;
      color: #444;
      text-align: center;
    }

    .envelope {
      width: 200px;
      height: 120px;
      background: #f8f8f8;
      border: 2px solid #ccc;
      position: relative;
      cursor: pointer;
      overflow: hidden;
      transform-style: preserve-3d;
      transition: all 0.6s ease;
    }

    .envelope.open {
      transform: rotateX(-180deg);
    }

    .envelope .flap {
      width: 200px;
      height: 60px;
      background: #ffb6c1;
      position: absolute;
      top: -2px;
      left: -2px;
      transform-origin: bottom;
      transform: rotateX(0deg);
      transition: all 0.6s ease;
    }

    .envelope.open .flap {
      transform: rotateX(-180deg);
    }

    .message {
      display: none;
      background: white;
      padding: 20px;
      margin-top: 20px;
      border-radius: 8px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
      max-width: 300px;
      font-size: 16px;
      line-height: 1.5;
      white-space: pre-wrap;
      animation: typing 3s steps(40, end);
      overflow: hidden;
      border-right: 3px solid;
    }

    @keyframes typing {
      from { width: 0 }
      to { width: 100% }
    }

    input {
      padding: 10px;
      font-size: 16px;
      border-radius: 5px;
      border: 1px solid #ccc;
      margin-bottom: 10px;
      width: 200px;
      text-align: center;
    }

    button {
      padding: 10px 20px;
      background: #f06292;
      color: white;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      font-size: 16px;
    }

    button:hover {
      background: #ec407a;
    }

    .error {
      color: #e91e63;
      font-size: 14px;
      margin-top: 10px;
    }

    /* Corazones flotantes */
    .heart {
      position: absolute;
      bottom: 0;
      width: 20px;
      height: 20px;
      font-size: 20px;
      color: #ff4081;
      opacity: 0.8;
      animation: floatUp 4s linear infinite;
    }

    @keyframes floatUp {
      0% { transform: translateY(0); opacity: 1; }
      100% { transform: translateY(-300px); opacity: 0; }
    }
  </style>
</head>
<body>

  <h1>Introduce la contraseña 🗝️</h1>
  <input type="password" id="passwordInput" placeholder="Escribe aquí...">
  <button onclick="checkPassword()">Desbloquear 💌</button>
  <div class="error" id="errorMessage"></div>

  <div class="envelope" id="envelope">
    <div class="flap"></div>
  </div>

  <div class="message" id="message"></div>

  <audio id="audio" src=""C:\Users\Maria\Desktop\pagina web yeiyei\Música emotiva #99 Historia romántica (No copyright).mp3""></audio>

  <script>
    const correctPassword = "11112023";
    const envelope = document.getElementById("envelope");
    const message = document.getElementById("message");
    const errorMessage = document.getElementById("errorMessage");
    const audio = document.getElementById("audio");

    const messageText = `Mi Yeiyei,
No puedo expresar con palabras lo agradecida que estoy por estos dos años.
Eres, sin duda, lo más bonito de mi vida.
Te amo con todo mi 💖`;

    function checkPassword() {
      const input = document.getElementById("passwordInput").value;
      if (input === correctPassword) {
        openEnvelope();
      } else {
        errorMessage.textContent = "Ups... esa no era. Intenta otra vez 💌";
      }
    }

    function openEnvelope() {
      envelope.classList.add("open");
      errorMessage.textContent = "";
      setTimeout(() => {
        message.style.display = "block";
        addHearts();
        playMusic();
        typeMessage();
      }, 800);
    }

    function typeMessage() {
      let i = 0;
      message.textContent = "";
      const interval = setInterval(() => {
        message.textContent += messageText[i];
        i++;
        if (i > messageText.length) clearInterval(interval);
      }, 50);
    }

    function addHearts() {
      for (let i = 0; i < 20; i++) {
        createHeart();
      }
    }

    function createHeart() {
      const heart = document.createElement("div");
      heart.className = "heart";
      heart.textContent = "❤";
      heart.style.left = `${Math.random() * 100}%`;
      heart.style.animationDuration = `${Math.random() * 2 + 3}s`;
      document.body.appendChild(heart);
      setTimeout(() => heart.remove(), 5000);
    }

    function playMusic() {
      audio.volume = 0.3;
      audio.play();
    }
  </script>

</body>
</html>
